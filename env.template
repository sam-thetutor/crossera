# CrossEra Environment Configuration

# Deployment Configuration
PRIVATE_KEY=your_private_key_here
CROSSFI_RPC_URL=https://rpc.testnet.crossfi.org
CROSSFI_CHAIN_ID=4157

# CrossFi Token Configuration
# For testnet, we'll use WXFI (Wrapped XFI) or native XFI handling
# Replace with actual XFI token contract address
XFI_TOKEN_ADDRESS=0x0000000000000000000000000000000000000000

# Contract Configuration
MIN_REWARD_AMOUNT=100000000000000000  # 0.1 XFI in wei
INITIAL_CAMPAIGN_POOL=1000000000000000000000  # 1000 XFI in wei

# Verification (when CrossFi block explorer supports it)
CROSSFI_API_KEY=your_crossfi_explorer_api_key

# Optional: Gas Configuration
GAS_PRICE=20000000000  # 20 gwei
GAS_LIMIT=8000000

# Admin Configuration
ADMIN_ADDRESS=  # Will use the deployer address if not specified
VERIFIER_ADDRESS=  # Address for the verifier service
